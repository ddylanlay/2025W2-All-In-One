// There should be tests here which check that the repository returns data in the correct format. This is an example file so it won't be filled with tests.

import { expect } from "@jest/globals";

test("example repository unit test", () => {
  expect(true).toBe(true);
});
